/*
package com.kleinpix.assignment7_back_end.repositories;

import com.kleinpix.assignment7_back_end.model.OrderLine;
import org.springframework.data.repository.CrudRepository;

public interface OrderLineRepository extends CrudRepository<OrderLine, Long> {

    Iterable<OrderLine> findById (String id);

    Iterable<OrderLine> findByQuantity (String quantity);

   OrderLine findByIdAndQuantity (String id, String quantity);
}
*/